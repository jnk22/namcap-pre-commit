#!/usr/bin/env sh

output=$(namcap "$@")

# Fail on output (errors) and set exit code for pre-commit compatibility
# since Namcap prints errors but always exits 0
if [ -n "$output" ]; then
  printf '%s\n' "$output"
  exit 1
fi
